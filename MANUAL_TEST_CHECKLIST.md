# Manual Test Checklist (V3 Firebase)

- Sign in with Google and confirm auth gate clears.
- Sign in with email/password (create + sign in).
- Watchlist loads from Firestore; edits persist after refresh.
- Sources load from Firestore; toggle active persists.
- Run monitoring creates report stubs and updates in real time.
- Saved report toggle sets/unsets saved and persists after refresh.
- Delete report removes it from the dashboard.
